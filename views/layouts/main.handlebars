<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>CodeChronicles</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/css/styles.css" />
</head>

<body>
    <header class="header-custom p-3">
        <nav class="navbar navbar-custom">
            <ul class="navbar-nav d-flex flex-row">
                <li class="nav-item">
                    <a class="nav-link nav-link-custom h4 mr-3" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link nav-link-custom h4 mr-3" href="/dashboard">Dashboard</a>
                </li>
                {{#unless loggedIn}}
                <li class="nav-item">
                    <a class="nav-link nav-link-custom h4 mr-3" href="/login">Login</a>
                </li>
                {{else}}
                <li class="nav-item">
                    <a class="nav-link nav-link-custom h4 mr-3" id="logout" href="#">Logout</a>
                </li>
                {{/unless}}
            </ul>
        </nav>
        <h2 class="text-left ml-2">{{pageTitle}}</h2>
    </header>
    {{{body}}}
    <script src='https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js'></script>
    <script src="/js/logout.js"></script>
</body>

</html>