<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>CodeChronicles</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" />
</head>

<body>
    <header class="bg-dark text-blue">
        <nav class="navbar navbar-dark bg-dark">
            <ul class="navbar-nav d-flex flex-row">
                <li class="nav-item">
                    <a class="nav-link h4 mr-3" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link h4 mr-3" href="/dashboard">Dashboard</a>
                </li>
                {{#unless loggedIn}}
                <li class="nav-item">
                    <a class="nav-link h4 mr-3" href="/login">Login</a>
                </li>
                {{else}}
                <li class="nav-item">
                    <a class="nav-link h4 mr-3" id="logout" href="#">Logout</a>
                </li>
                {{/unless}}
            </ul>
        </nav>
        <h2 class="text-left ml-2">{{pageTitle}}</h2>
    </header>
    {{{body}}}
    <script src='https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js'></script>
    <script src="/js/logout.js"></script>
</body>
</html>